spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:marginalia}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8

app:
  auth:
    dev-mode: true
    whitelist-emails: ${AUTH_WHITELIST_EMAILS:test@example.com}
    session:
      secure-cookie: false
